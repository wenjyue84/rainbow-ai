{
  "productName": "Rainbow AI — FnB AI Waiter Enhancement",
  "branchName": "master",
  "overview": "End-to-end AI waiter integration: rainbow-ai makan-moments profile handles customer chat from fnb-online-ordering, acting as MCP client to query live menu data and place orders in the fnb-online-ordering system. Customers at localhost:3031/en can chat with the AI waiter, ask about the menu, and place pre-orders — all powered by rainbow-ai's makan-moments profile calling back to fnb-online-ordering's MCP server.",
  "goals": [
    "Add /api/fnb/chat SSE endpoint in rainbow-ai so the fnb-online-ordering proxy can connect",
    "Add FnB MCP client tools in rainbow-ai (menu browsing, order creation, order status)",
    "Register FnB tools for makan-moments profile only, not hostel profiles",
    "Configure makan-moments profile to use FnB tools when answering customer questions",
    "Fix and verify end-to-end: customer chat → rainbow-ai → MCP call → live fnb data",
    "Enhance fnb-online-ordering chat UI with order-aware quick replies and better UX"
  ],
  "userStories": [
    {
      "id": "US-601",
      "title": "Add /api/fnb/chat SSE streaming endpoint in rainbow-ai",
      "priority": "high",
      "description": "The fnb-online-ordering proxy at /api/chat/rainbow calls RAINBOW_AI_URL/api/fnb/chat but this endpoint does not exist in rainbow-ai yet. Create a POST /api/fnb/chat route that: accepts { messages: [{id,role,content,parts}] } (UIMessage array from Vercel AI SDK), routes to the makan-moments profile via profileRegistry, streams SSE events in format {\"type\":\"text\",\"text\":\"...\"} terminated with [DONE], and uses the existing processChat / chat-engine. Also accept the simpler { message: string, history: [], sessionId? } format for backwards compatibility.",
      "acceptanceCriteria": [
        "POST http://localhost:3002/api/fnb/chat with {\"messages\":[{\"id\":\"1\",\"role\":\"user\",\"content\":\"hello\",\"parts\":[{\"type\":\"text\",\"text\":\"hello\"}]}]} returns SSE stream",
        "SSE events are in format: data: {\"type\":\"text\",\"text\":\"...\"}\\n\\n",
        "Stream ends with data: [DONE]\\n\\n",
        "Uses makan-moments profile (not pelangi or southern)",
        "Also accepts legacy { message: string, history: [] } format and converts to UIMessage[]",
        "Rate limited to 10 req/min per IP (reuse webchat limiter)",
        "TypeScript compiles without errors"
      ],
      "technicalNotes": [
        "Add the route in src/routes/public/webchat-api.ts or a new src/routes/public/fnb-chat.ts",
        "Mount at app.use('/api/fnb', fnbChatRoutes) in src/index.ts",
        "Reuse profileRegistry.getProfile('makan-moments') and the existing processChat function from chat-engine.ts",
        "SSE format must match what fnb-online-ordering/src/app/api/chat/rainbow/route.ts expects: it reads data: lines, parses JSON, looks for parsed.type === 'text' && parsed.text",
        "For streaming: use res.setHeader('Content-Type','text/event-stream'), write chunks as they come from the LLM",
        "If processChat does not stream natively, call it and write the full response as a single SSE event",
        "Add FNB_CHAT_RATE_LIMIT env var (default 10) for configurable rate limit"
      ],
      "dependencies": [],
      "estimatedComplexity": "medium",
      "passes": false
    },
    {
      "id": "US-605",
      "title": "Verify /api/fnb/chat connects to makan-moments profile with FnB MCP tools end-to-end",
      "priority": "high",
      "description": "Full integration test: start rainbow-ai dev server, call /api/fnb/chat, verify it uses makan-moments profile, verify the AI can call fnb_get_menu tool and return live menu data. Fix any wiring issues discovered. Also verify the fnb-online-ordering proxy (/api/chat/rainbow) successfully connects when RAINBOW_AI_URL=http://localhost:3002. Test message: 'What's on the menu?' should result in AI calling fnb_get_menu and returning real menu items.",
      "acceptanceCriteria": [
        "POST /api/fnb/chat with 'What's on the menu?' triggers fnb_get_menu tool call in rainbow-ai logs",
        "Response contains actual menu items from fnb-online-ordering database",
        "No TypeScript build errors in rainbow-ai",
        "fnb-online-ordering can build without errors (npm run build in that project)",
        "curl test documented: curl -X POST http://localhost:3002/api/fnb/chat -H 'Content-Type: application/json' -d '{\"message\":\"what is on the menu\",\"history\":[]}' returns SSE with menu data"
      ],
      "technicalNotes": [
        "This story is integration/wiring — fix any issues found when running the actual test",
        "Common issues to check: CORS, tool execution not happening, profile not found, MCP URL wrong",
        "Add FNB_MCP_URL=http://localhost:3031/api/mcp to .env.pelangi.local example (or .env.example)",
        "Ensure rainbow-ai's makan-moments profile calls tools during chat (check workflow-executor.ts or chat-engine.ts for tool calling)",
        "If tools are not being called, may need to enable tool_use mode in makan-moments AI settings"
      ],
      "dependencies": [
        "US-601",
        "US-604"
      ],
      "estimatedComplexity": "medium",
      "passes": false
    },
    {
      "id": "US-606",
      "title": "Enhance fnb-online-ordering AI waiter chat UI with order-aware quick replies",
      "priority": "medium",
      "description": "Update the fnb-online-ordering chat widget to be more order-aware. Update the QuickReplies component at src/components/chat/quick-replies.tsx to include FnB-specific suggestions: 'Show me the menu', 'What are today's specials?', 'Place an order', 'Check my order status', 'What time do you open?'. Also update chat-panel.tsx to: (1) show an 'Add items to tray' button when the AI mentions specific menu items in its response, (2) display a visual order confirmation step when the AI creates an order. The chat should feel like a natural ordering assistant.",
      "acceptanceCriteria": [
        "QuickReplies shows 'Show me the menu' and 'Place an order' as initial suggestions",
        "After AI mentions menu items, quick replies update to context-aware suggestions",
        "When AI creates an order (response mentions order ID MM-XXXX), a confirmation card appears in chat",
        "Order confirmation card shows: order ID, items, total, and a 'Track my order' link",
        "Chat placeholder text updated to 'Ask about the menu or place an order...'",
        "No TypeScript errors in fnb-online-ordering"
      ],
      "technicalNotes": [
        "File: src/components/chat/quick-replies.tsx — update initial quick reply buttons",
        "File: src/components/chat/chat-panel.tsx — add order confirmation parsing",
        "File: src/components/chat/chat-bubble.tsx — optionally add special rendering for order confirmations",
        "Parse AI response for order ID pattern MM-[A-Z0-9]{4} and render a card",
        "Use the existing tray state to pre-fill items if the AI suggests adding to order",
        "The NEXT_PUBLIC_RAINBOW_AI_URL env var in fnb-online-ordering may need updating (currently used as link in chat header)"
      ],
      "dependencies": [
        "US-605"
      ],
      "estimatedComplexity": "medium",
      "passes": false
    },
    {
      "id": "US-607",
      "title": "Add .env.example documentation for FnB AI waiter integration in rainbow-ai",
      "priority": "low",
      "description": "Update rainbow-ai's .env.example to document all environment variables needed for the FnB AI waiter integration: FNB_MCP_URL (URL of fnb-online-ordering /api/mcp endpoint), FNB_MCP_SECRET (shared secret matching FNB_MCP_SECRET in fnb-online-ordering), FNB_CHAT_RATE_LIMIT (max chat requests per minute, default 10). Also update WIDGET_SETTINGS.md (untracked file in rainbow-ai) with instructions for embedding the AI waiter in fnb-online-ordering.",
      "acceptanceCriteria": [
        ".env.example has FNB_MCP_URL with comment explaining its purpose",
        ".env.example has FNB_MCP_SECRET with comment",
        "WIDGET_SETTINGS.md documents how to connect fnb-online-ordering to rainbow-ai",
        "README or WIDGET_SETTINGS.md has a quick-start curl test for /api/fnb/chat"
      ],
      "technicalNotes": [
        "Check existing .env.example in rainbow-ai root",
        "WIDGET_SETTINGS.md is already an untracked file — update it",
        "Add a section: FnB AI Waiter Integration with step-by-step setup"
      ],
      "dependencies": [
        "US-605"
      ],
      "estimatedComplexity": "small",
      "passes": false
    }
  ]
}
