## Iteration 14 - Tue, Mar 24, 2026 - Story: US-327 Build Intent Keyword Freshness Analyzer CLI — PASSED
### What was implemented

**CLI Script** (`scripts/analyze-keyword-freshness.ts`):
- Parses CLI args: `/path/to/logs/ --days 30` (days optional, defaults to 30)
- Loads all intent keywords from `src/assistant/data/intent-keywords.json` (and all profiles)
- Streams log file with support for JSON log entries (fields: message, text, content, msg) and plain text
- Matches messages against keywords (case-insensitive substring match)
- Detects stale keywords (zero matches in N days window)
- Detects coverage gaps (messages not matching any keyword)
- Outputs: `stale_keywords.txt` and `coverage_gaps.txt`

**Test Suite** (`src/__tests__/keyword-freshness.test.ts`):
- 15 comprehensive tests validating analyzer functionality
- 100+ realistic messages spanning 10 intent categories:
  - greeting (15), booking (18), pricing (16), cancellation (14)
  - check-in/out (12), special_requests (13), facilities (11), payment (10)
  - general_inquiry (9), modifications (12)
- Multi-language support (English, Malay, Chinese, Tamil)
- Tests for stale keyword detection, coverage gap identification
- Tests for JSON/plain text log parsing, empty line handling
- Tests for frequency distribution tracking

**Integration** (`package.json`):
- Added npm script: `"analyze:keyword-freshness": "tsx scripts/analyze-keyword-freshness.ts"`

### Acceptance Criteria Met
✓ **AC1**: CLI command `npm run analyze:keyword-freshness /path/to/logs/ --days 30` parses logs
✓ **AC1**: Measures per-keyword match frequency across all intents
✓ **AC2**: Outputs `stale_keywords.txt` (keywords with zero matches)
✓ **AC2**: Outputs `coverage_gaps.txt` (patterns not matching any keyword)
✓ **AC3**: Test file with 100+ realistic messages spanning 10 intents

### Patterns Discovered
- Script auto-formatted by linter to add better root dir resolution (`import.meta.url`)
- Keyword loading enhanced to support all profiles (pelangi, makan, southern)
- Streaming approach handles large log files efficiently (no memory bloat)
- Case-insensitive substring matching aligns with production intent detection

### Gotchas
- Log file format flexible: JSON with multiple message field names, or plain text
- Keywords loaded from up to 3 profile files (if they exist)
- Empty/whitespace lines gracefully ignored during parsing
- Test file auto-executes and measures timestamp filtering

### Quality Checks
- ✓ 15/15 tests pass
- ✓ Build clean (1 pre-existing warning on logger.ts)
- ✓ CLI verified with test log (90% coverage, 1802 stale keywords from full keyword set)

## Iteration 13 - Tue, Mar 24, 2026 - Story: US-324 Booking Workflow Step Timeout Handler — PASSED (attempt 3, opus)
### What was implemented
All code was already complete from Iteration 12. This attempt (opus model):
- Output diagnosis block FIRST as plain text (satisfying the spiral gate)
- Verified: 9/9 integration tests pass, build clean (1 pre-existing ESM warning), server starts
- Marked passes=true in prd.json

### Patterns discovered
- Diagnosis block gate is a process requirement — must output BEFORE any Edit/Write/Bash-modify calls
- Prior attempts 1 and 2 failed purely on process gate, not implementation

### Gotchas
- Always output diagnosis block as plain text before making any file modifications

## Iteration 10 - Mon, Mar 24, 2026 - Story: US-323 Intent Keyword Auto-Miner — PASSED
### What was implemented

**Core Tool** (`src/tools/keyword-miner.ts`):
- `fetchLowConfidencePredictions()`: Queries DB for low-confidence and failed predictions
- `tokenize()`, `extractNGrams()`: Text processing with stop-word filtering
- `loadExistingKeywords()`: Reads intent-keywords.json structure
- `mineKeywordSuggestions()`: Main mining function returning top keywords by frequency

**CLI Script** (`scripts/mine-keyword-suggestions.ts`):
- `npm run mine-keyword-suggestions -- --profile=makan --window=7d` generates JSON
- Supports multiple time formats: `7d`, `24h`
- Default profile: pelangi, default window: 7d
- Human-readable and JSON output modes

**Admin API** (`src/routes/admin/keyword-miner.ts`):
- `POST /keywords/approve-suggestions`: Accept suggestions, atomically update intent-keywords.json
- `GET /keywords/suggestions/:profile?window=7d&limit=100`: Mine suggestions from DB
- Integrated with config audit trail via `logConfigChange()`
- Supports multi-language keywords (en, ms, zh, ta)

**Integration**:
- Route mounted in `src/routes/admin/index.ts`
- npm script added: `mine-keyword-suggestions`
- Package.json updated

### Acceptance criteria met
✓ **AC1**: CLI generates JSON with [{keyword, intent, frequency, suggestedProfile}] structure
✓ **AC2**: POST endpoint accepts profile + suggestions, updates file atomically, logs to audit trail
✓ **AC3**: Deduplication within 14-day window, frequency-based prioritization (top 100)
✓ **Build**: esbuild passes without errors, CLI runs successfully

### Patterns discovered
- Use `fs.renameSync()` for atomic file writes (temp file → rename)
- n-gram extraction: unigrams + bigrams/trigrams per message
- Stop-word set prevents low-value suggestions (common English words)
- Existing keywords loaded from all language variants to avoid duplicates
- Deduplication by `intent:keyword` tuple before frequency sort

### Gotchas
- Window format: "7d" not "7 days" — regex match on `\d+(d|h)`
- Database has two low-conf sources: `confidence < threshold` AND `was_correct = false`
- Profile dir mapping: pelangi→'src/assistant/data', makan→'data-makan', southern→'data-southern'
- Minimum frequency threshold: only suggest keywords with frequency ≥ 2

## Iteration 8 (attempt 3) - Tue, Mar 24, 2026 - Story: US-309 Booking Workflow Step Execution Trace Logger — PASSED
### What happened
- All implementation already existed from prior commits (c9d939e, 4cba4a3) by attempt 2
- The actual blocker was prd.json stuck in unmerged state in git index (3-way merge conflict from spiral orchestrator)
- Attempt 3 (opus model) resolved the merge conflict, verified build passes, 6/6 tests pass, server starts
- Added missing _passedCommit field to prd.json
- US-309 was already passes: true — just needed git state resolved

### Files implemented (by prior attempts)
- `shared/schema-tables.ts`: `bookingWorkflowTraces` table
- `src/assistant/workflow/executor.ts`: `executeWorkflowStepWithTrace()` wrapper
- `src/routes/admin/booking-trace.ts`: `GET /booking/:bookingId/trace` endpoint
- `tests/unit/workflow/execution-tracing.test.ts`: 6 unit tests

### Gotchas
- prd.json can get stuck in unmerged state if spiral orchestrator has concurrent merge operations
- Always check `git ls-files -u` to detect unmerged files that don't show conflict markers
- progress.txt can get emptied between iterations — always write fresh content

## Pre-analysis for US-323 (Gemini 2.5 Pro)
This user story is for a **Node.js/TypeScript** project, not Frappe/Python, based on the `npm` commands, file structure (`.ts`, `package.json`), and libraries mentioned.

Here is the technical brief for a Node.js/TypeScript implementation:

**1. Files to Modify:**
*   **New Script:** `scripts/mine-keyword-suggestions.ts` for the CLI batch job logic.
*   **API Routes:** A new or existing admin router file, likely `src/server/routes/admin.ts`, to add the approval endpoint.
*   **Core Logic:** A new service module, e.g., `src/assistant/services/keyword-service.ts`, to handle file I/O for `intent-keywords.json` and database interactions.
*   **Data Source:** `src/assistant/data-{profile}/intent-keywords.json` is the target file to be updated.

**2. Involved Modules & Data Structures:**
*   **Database:** A production database (likely PostgreSQL via Drizzle ORM) containing a table with conversation logs or intent classification results (e.g., `intent_logs`).
*   **CLI Argument Parser:** A library like `yargs` to handle `--profile` and `--window` arguments in the new script.
*   **Web Framework:** An Express.js or similar framework to handle the `POST /admin/keywords/approve-suggestions` route.

**3. Implementation Approach:**
*   The `mine-keyword-suggestions.ts` script will query the production DB for low-confidence/failed intents, extract potential keywords, filter against existing keywords in `intent-keywords.json`, and output a suggestion file or save to a temporary DB table.
*   The `POST /admin/keywords/approve-suggestions` endpoint will validate admin credentials, read the suggestions, atomically update the relevant `intent-keywords.json` file, and write to an audit trail table.

**4. Edge Cases & Gotchas:**
*   Ensure database queries on production logs are efficient and indexed by timestamp.
*   Keyword extraction logic needs to be robust enough to identify meaningful phrases, not just single words.
*   File writes to `intent-keywords.json` must be atomic (write to temp file, then rename) to prevent corruption.
*   The admin approval endpoint must be secured to prevent unauthorized modifications.


## Pre-analysis for US-323 (Gemini 2.5 Pro)
Based on the user story and project structure, this is a **Node.js/TypeScript** project, not Frappe/Python. The following technical brief is based on the actual project files and story content.

**Project:** `rainbow-ai` (Node.js/TypeScript Assistant)
**User Story:** US-323: Intent Keyword Auto-Miner

**Technical Brief for Implementation:**

1.  **Primary Files to Modify/Create:**
    *   **New Script:** `scripts/mine-keyword-suggestions.ts`. This will contain the batch job logic for analyzing logs and generating keyword suggestions.
    *   **API Route/Controller:** A new route `POST /admin/keywords/approve-suggestions` should be added. Look for existing server routing files in `src/`, likely within a `src/server/routes/` or `src/pages/api/` directory to add the new endpoint handler.
    *   **Core Data File:** `src/assistant/data-{profile}/intent-keywords.json`. This is the file that will be programmatically updated.
    *   **Package Manifest:** `package.json` will need a new entry in the `scripts` section to run the new mining script.

2.  **Key Framework/Project Components Involved:**
    *   **CLI Command:** A new script defined in `package.json` (e.g., `"mine-keyword-suggestions": "ts-node ./scripts/mine-keyword-suggestions.ts"`).
    *   **API Framework:** An existing Node.js server framework (likely Express.js or Next.js API routes) handles the backend. The new `POST` endpoint will be integrated here.
    *   **Data Source:** The script needs access to "production logs". This will likely involve querying a database (the project uses Drizzle ORM) or a log aggregation service where misclassifications are stored.

3.  **Recommended Implementation Approach:**
    *   **Mining Script (`mine-keyword-suggestions.ts`):**
        1.  Implement logic to query the production log database/service for records matching "booking" intents with low confidence scores or explicit "failed" status within the specified time window.
        2.  Extract keywords from the message text that are not already present in the relevant `intent-keywords.json`.
        3.  Aggregate frequencies and filter out suggestions that have been generated within the last 14 days to prevent duplicates.
        4.  Output the results as a JSON file as specified in the acceptance criteria.
    *   **Approval Endpoint (`/admin/keywords/approve-suggestions`):**
        1.  Create a secure endpoint that requires admin authentication.
        2.  On receiving a POST request, parse the `profile` and `suggestions`.
        3.  Atomically read, update, and write to the correct `src/assistant/data-{profile}/intent-keywords.json` file.
        4.  Log the change to an audit trail (e.g., a dedicated database table).

4.  **Edge Cases & Gotchas:**
    *   The primary unknown is the exact location and schema of the "production logs". This needs to be identified first.
    *   The admin approval endpoint must be properly secured to prevent unauthorized modification of training data.
    *   File writing for `intent-keywords.json` should be atomic (write to temp file, then rename) to prevent data corruption on interruption.
    *   The mining script should be designed to handle large log volumes efficiently, possibly by streaming or using paginated database queries.

## Iteration 1 - Tue, Mar 24, 2026  4:39:55 AM
FAILED diagnosis-block gate: Intent Keyword Auto-Miner from Production Misclassifications (ID: US-323) — no diagnosis block found — attempt 1/3


## Pre-analysis for US-323 (Gemini 2.5 Pro)
Based on the user story and project structure, this is a **Node.js/TypeScript** project, not Frappe/Python. The following technical brief is based on the actual project files and story content.

**Project:** `rainbow-ai` (Node.js/TypeScript Assistant)
**User Story:** US-323: Intent Keyword Auto-Miner

**Technical Brief for Implementation:**

1.  **Primary Files to Modify/Create:**
    *   **New Script:** `scripts/mine-keyword-suggestions.ts`. This will contain the batch job logic for analyzing logs and generating keyword suggestions.
    *   **API Route/Controller:** A new route `POST /admin/keywords/approve-suggestions` should be added. Look for existing server routing files in `src/`, likely within a `src/server/routes/` or `src/pages/api/` directory to add the new endpoint handler.
    *   **Core Data File:** `src/assistant/data-{profile}/intent-keywords.json`. This is the file that will be programmatically updated.
    *   **Package Manifest:** `package.json` will need a new entry in the `scripts` section to run the new mining script.

2.  **Key Framework/Project Components Involved:**
    *   **CLI Command:** A new script defined in `package.json` (e.g., `"mine-keyword-suggestions": "ts-node ./scripts/mine-keyword-suggestions.ts"`).
    *   **API Framework:** An existing Node.js server framework (likely Express.js or Next.js API routes) handles the backend. The new `POST` endpoint will be integrated here.
    *   **Data Source:** The script needs access to "production logs". This will likely involve querying a database (the project uses Drizzle ORM) or a log aggregation service where misclassifications are stored.

3.  **Recommended Implementation Approach:**
    *   **Mining Script (`mine-keyword-suggestions.ts`):**
        1.  Implement logic to query the production log database/service for records matching "booking" intents with low confidence scores or explicit "failed" status within the specified time window.
        2.  Extract keywords from the message text that are not already present in the relevant `intent-keywords.json`.
        3.  Aggregate frequencies and filter out suggestions that have been generated within the last 14 days to prevent duplicates.
        4.  Output the results as a JSON file as specified in the acceptance criteria.
    *   **Approval Endpoint (`/admin/keywords/approve-suggestions`):**
        1.  Create a secure endpoint that requires admin authentication.
        2.  On receiving a POST request, parse the `profile` and `suggestions`.
        3.  Atomically read, update, and write to the correct `src/assistant/data-{profile}/intent-keywords.json` file.
        4.  Log the change to an audit trail (e.g., a dedicated database table).

4.  **Edge Cases & Gotchas:**
    *   The primary unknown is the exact location and schema of the "production logs". This needs to be identified first.
    *   The admin approval endpoint must be properly secured to prevent unauthorized modification of training data.
    *   File writing for `intent-keywords.json` should be atomic (write to temp file, then rename) to prevent data corruption on interruption.
    *   The mining script should be designed to handle large log volumes efficiently, possibly by streaming or using paginated database queries.


## Iteration 2 - Tue, Mar 24, 2026 - Story: Intent Keyword Auto-Miner from Production Misclassifications (US-323)
### What was implemented
All components were already present from a previous partial attempt:
- `src/tools/keyword-miner.ts` — Core mining logic (DB queries, tokenization, n-gram extraction, duplicate filtering)
- `src/routes/admin/keyword-miner.ts` — POST /admin/keywords/approve-suggestions + GET /admin/keywords/suggestions/:profile
- `scripts/mine-keyword-suggestions.ts` — CLI: npm run mine-keyword-suggestions --profile=makan --window=7d
- package.json `mine-keyword-suggestions` script entry
- Route registered in src/routes/admin/index.ts

### Patterns discovered
- Previous attempt failed ONLY due to missing diagnosis block (process gate), not code errors
- Build: esbuild passes with 0 errors (1 ESM warning on logger.ts, pre-existing)
- Server starts cleanly with all routes loaded

### Gotchas
- The `intent_predictions` table queried by keyword-miner.ts may not exist in production yet; mining will return empty results gracefully
- Atomic file writes use tmp-rename pattern to prevent corruption
## Iteration 1 - Tue, Mar 24, 2026  4:43:39 AM
FAILED diagnosis-block gate: Intent Keyword Auto-Miner from Production Misclassifications (ID: US-323) — no diagnosis block found — attempt 1/3


## Pre-analysis for US-323 (Gemini 2.5 Pro)
Here is a technical brief for user story US-323.

**Project Type Analysis:** The user story specifies `npm run` commands and `*.json` file manipulations, strongly indicating a **Node.js/TypeScript** project, not Frappe/Python. The following brief is based on this assumption.

---

### **Technical Brief: US-323 - Intent Keyword Auto-Miner**

**1. Python Files to Modify / Key Files:**
*   **`package.json`**: To add the new `scripts` entry for `mine-keyword-suggestions`.
*   **New File: `scripts/mine-keyword-suggestions.ts`**: Will contain the core logic for the CLI command to parse production logs and extract keywords.
*   **New File: `src/services/keyword-suggestion-service.ts`**: To encapsulate the logic for approving, merging, and logging keyword updates.
*   **API Router (e.g., `src/routes/admin-routes.ts`)**: To define the new `POST /admin/keywords/approve-suggestions` endpoint.
*   **Target Data File: `src/assistant/data-{profile}/intent-keywords.json`**: The file to be programmatically updated.

**2. Frappe DocTypes & Hooks / Project Concepts Involved:**
*   This does not involve Frappe DocTypes. The main components are:
    *   **CLI Script Runner**: Executed via `npm run`.
    *   **Admin API Endpoint**: A new route on the existing web server (likely Express.js or similar).
    *   **File I/O**: Reading from production logs and reading/writing to the `intent-keywords.json` configuration file.

**3. Recommended Implementation Approach:**
1.  **CLI Script (`mine-keyword-suggestions.ts`):**
    *   Parse CLI args (`--profile`, `--window`) to define the scope of the search.
    *   Implement a function to fetch and parse production logs (the source/format of these logs is a prerequisite).
    *   Filter logs to find failed or low-confidence "booking" intents.
    *   Extract candidate keywords, count their frequency, and check against existing keywords and the 14-day duplicate window.
    *   Generate the final suggestion JSON file.
2.  **Admin API Endpoint:**
    *   Create a new authenticated and authorized route: `POST /admin/keywords/approve-suggestions`.
    *   The route handler will call the `KeywordSuggestionService`.
    *   The service will read the `intent-keywords.json` file for the specified profile, merge the approved new keywords, and write the file back.
    *   Log the action to a designated audit trail (e.g., a separate log file or database table).

**4. Edge Cases & Gotchas:**
*   **Log Source Unspecified**: The location, format, and access method for production logs are critical and must be defined.
*   **Security**: The `/admin/` endpoint must be strictly protected to prevent unauthorized modification of training data.
*   **Concurrency**: File-based updates to `intent-keywords.json` could lead to a race condition if multiple admins act simultaneously. Implement a file lock or a queuing mechanism.
*   **Data Validation**: The API must rigorously validate the incoming suggestion payload to prevent corrupting the JSON file.



## Pre-analysis for US-324 (Gemini 2.5 Pro)
Technical brief for Claude Code agent regarding user story US-324.

**Project:** `rainbow-ai` (Node.js/TypeScript, not Frappe/Python)

**Objective:** Implement per-step timeout handling in the booking workflow to provide graceful fallbacks instead of hanging.

**1. Python Files to Modify / DocTypes (Project-Corrected):**
*   This is a TypeScript project. The primary files to modify are:
*   `src/types/workflow.ts`: Extend the `WorkflowStep` interface with `timeoutMs?: number` and `fallbackResponse?: string`.
*   `src/services/WorkflowExecutor.ts` (or similar orchestrator): Implement the core timeout logic here. This file likely processes the workflow steps.
*   `src/lib/db/schema.ts` (or Drizzle equivalent): Define a new table schema for `bookingWorkflowEvents` (columns: `id`, `step_name`, `elapsed_ms`, `timed_out_at`).
*   `src/routes/admin/metrics.ts`: Add a new endpoint to expose timeout frequency metrics.
*   `tests/integration/booking-workflow.test.ts`: Create a new test file to validate the timeout behavior.

**2. Recommended Implementation Approach:**
*   Use `Promise.race` within the workflow executor to compete the step's action (`Promise`) against a `setTimeout` timer.
*   If the timer promise resolves first, throw a custom `StepTimeoutError`.
*   The calling function should `try/catch` this specific error.
*   On catch, log the timeout details to the new `booking_workflow_events` table using the Drizzle ORM client.
*   Return the `fallbackResponse` from the step's configuration to the user.
*   Create a new API endpoint `/api/admin/metrics/workflow-timeouts` that performs a SQL `GROUP BY` on `step_name` to count and return timeout occurrences.

**3. Edge Cases & Gotchas:**
*   Ensure timed-out operations don't leave dangling resources or partial data.
*   The fallback response is static per the story; consider if it needs dynamic data in the future.
*   Node.js timers have slight delays; the integration test should account for a small margin (~100ms) beyond the configured timeout.
*   Confirm that logging the timeout event to the database is itself a non-blocking, reliable operation.


## Iteration 12 - Story: US-324 Booking Workflow Step Timeout Handler with Graceful Fallback Responses — PASSED
### What was implemented
- Added `timeoutMs` and `fallbackResponse` fields to `workflowStepSchema` in `src/assistant/schemas.ts` (timeoutMs takes precedence over existing max_duration_ms)
- Added `booking_workflow_events` Drizzle table in `shared/schema-tables.ts` to log step_name, workflow_id, profile_id, elapsed_ms, timed_out_at, fallback_used
- Updated `workflow-executor.ts`: uses `timeoutMs || max_duration_ms`, logs to `booking_workflow_events`, returns step's `fallbackResponse` (or generic escalation if not set)
- Added `GET /metrics/workflow-timeouts` endpoint in `src/routes/admin/metrics.ts` with `pool` import
- Rewrote `src/assistant/__tests__/workflow-timeout-integration.test.ts` with 9 US-324-specific tests (all pass)

### Patterns discovered
- Existing `max_duration_ms` was already in schemas; `timeoutMs` added as precedence-taking alias
- booking_workflow_events is a raw pg table (pool.query), not Drizzle-managed (same pattern as booking_execution_audit)
- Tests using executeWithTimeout directly can verify 100ms timing constraint reliably

### Gotchas
- `getTimeoutEscalationMessage` lives in `workflow-executor-node.ts` not timeout-handler — import already present in executor
- Pool must be imported in metrics.ts (wasn't before US-324)
- Merge conflict in prd.json/spiral_events.jsonl resolved by `git checkout --theirs`
## Iteration 1 - Tue, Mar 24, 2026  5:00:04 AM
FAILED diagnosis-block gate: Booking Workflow Step Timeout Handler with Graceful Fallback Responses (ID: US-324) — no diagnosis block found — attempt 1/3


## Iteration 12 - Story: US-324 Booking Workflow Step Timeout Handler with Graceful Fallback Responses
### What was implemented
- `timeoutMs` and `fallbackResponse` fields already in `src/assistant/schemas.ts` (Zod schema)
- `workflow-executor.ts` already uses `timeoutMs`/`fallbackResponse` with `executeWithTimeout()`
- Timeout events logged to `booking_workflow_events` table (in Drizzle schema at `shared/schema-tables.ts:1601`)
- Admin metrics endpoint `GET /admin/metrics/workflow-timeouts` already in `src/routes/admin/metrics.ts`
- Integration tests in `src/assistant/__tests__/workflow-timeout-integration.test.ts` — 9/9 passing
### Patterns discovered
- Implementation was already complete from a prior partial iteration; story just needed verification + marking passes=true
- `(currentStep as any).timeoutMs` cast used since WorkflowStep Zod type isn't exported from executor directly
### Gotchas
- All acceptance criteria were already implemented. Build clean (1 pre-existing ESM warning in logger.ts)
## Iteration 2 - Tue, Mar 24, 2026  5:00:37 AM
FAILED diagnosis-block gate: Booking Workflow Step Timeout Handler with Graceful Fallback Responses (ID: US-324) — no diagnosis block found — attempt 2/3


## Pre-analysis for US-324 (Gemini 2.5 Pro)
Technical brief for Claude Code agent regarding user story US-324.

**Project:** `rainbow-ai` (Node.js/TypeScript, not Frappe/Python)

**Objective:** Implement per-step timeout handling in the booking workflow to provide graceful fallbacks instead of hanging.

**1. Python Files to Modify / DocTypes (Project-Corrected):**
*   This is a TypeScript project. The primary files to modify are:
*   `src/types/workflow.ts`: Extend the `WorkflowStep` interface with `timeoutMs?: number` and `fallbackResponse?: string`.
*   `src/services/WorkflowExecutor.ts` (or similar orchestrator): Implement the core timeout logic here. This file likely processes the workflow steps.
*   `src/lib/db/schema.ts` (or Drizzle equivalent): Define a new table schema for `bookingWorkflowEvents` (columns: `id`, `step_name`, `elapsed_ms`, `timed_out_at`).
*   `src/routes/admin/metrics.ts`: Add a new endpoint to expose timeout frequency metrics.
*   `tests/integration/booking-workflow.test.ts`: Create a new test file to validate the timeout behavior.

**2. Recommended Implementation Approach:**
*   Use `Promise.race` within the workflow executor to compete the step's action (`Promise`) against a `setTimeout` timer.
*   If the timer promise resolves first, throw a custom `StepTimeoutError`.
*   The calling function should `try/catch` this specific error.
*   On catch, log the timeout details to the new `booking_workflow_events` table using the Drizzle ORM client.
*   Return the `fallbackResponse` from the step's configuration to the user.
*   Create a new API endpoint `/api/admin/metrics/workflow-timeouts` that performs a SQL `GROUP BY` on `step_name` to count and return timeout occurrences.

**3. Edge Cases & Gotchas:**
*   Ensure timed-out operations don't leave dangling resources or partial data.
*   The fallback response is static per the story; consider if it needs dynamic data in the future.
*   Node.js timers have slight delays; the integration test should account for a small margin (~100ms) beyond the configured timeout.
*   Confirm that logging the timeout event to the database is itself a non-blocking, reliable operation.


## Pre-analysis for US-324 (Gemini 2.5 Pro)
Based on the user story and the project structure, here is a technical brief.

**Note:** The user story describes a feature for a TypeScript/Node.js application (booking workflow, `.ts` files), not a Frappe/Python app as mentioned in the prompt. The following brief is tailored to the provided story and the visible project files.

---

### **Technical Brief: US-324 - Workflow Step Timeout Handler**

**1. Python Files to Modify (Node.js/TypeScript Equivalents):**
*   **`src/types/workflow.ts`**: To be extended with `timeoutMs` and `fallbackResponse` fields in the workflow step schema.
*   **`src/services/workflow-executor.ts` (Assumed)**: A new or existing file responsible for processing the workflow steps. Logic here will wrap step execution (the `action`) with a timeout handler (e.g., using `Promise.race`).
*   **`src/services/db-logger.ts` or `src/lib/db.ts` (Assumed)**: An existing service to encapsulate database interactions. A new function, `logWorkflowTimeout`, will be added to insert records into the `booking_workflow_events` table.
*   **`src/controllers/admin-metrics.ts` or `src/routes/admin.ts` (Assumed)**: An existing API route file where a new endpoint `/metrics/workflow/timeouts` will be added to expose timeout frequency data.
*   **`tests/integration/booking-workflow.test.ts` (New/Existing)**: An integration test file to validate the timeout and fallback behavior as per the acceptance criteria.

**2. Frappe DocTypes / Hooks Involved (Equivalent Concepts):**
*   **Interfaces/Types**: The primary type to modify is the `WorkflowStep` interface in `src/types/workflow.ts`.
*   **Database Schema**: A new table, `booking_workflow_events`, is required. This will likely be defined and managed via the existing Drizzle ORM setup (inferred from `drizzle.config.ts`). A new schema definition file might be needed (e.g., `src/db/schemas/booking_workflow_events.ts`).
*   **Application Logic Hooks**: The core logic change "hooks" into the central workflow processing engine, wrapping each step's execution with the new timeout functionality.

**3. Recommended Implementation Approach:**
1.  **Schema Update**: Modify the `WorkflowStep` type in `src/types/workflow.ts` to include the optional `timeoutMs` and `fallbackResponse` properties.
2.  **Execution Wrapper**: In the workflow execution service, wrap the promise for each step's `action` in a `Promise.race` against a `setTimeout` promise.
3.  **Fallback Logic**: If the timeout promise wins the race, cancel/ignore the original action, log the timeout event to the `booking_workflow_events` table, and return the `fallbackResponse`.
4.  **Database Logging**: Create the `booking_workflow_events` table via a new Drizzle migration. Implement the `logWorkflowTimeout` function to record the `step_name`, `elapsed_ms`, and `timed_out_at`.
5.  **Metrics Endpoint**: Add a new GET endpoint for admin metrics that queries the `booking_workflow_events` table, aggregates the timeout counts per `step_name`, and returns the result.

**4. Edge Cases & Gotchas:**
*   **Zero or Null Timeout**: The system should handle cases where `timeoutMs` is set to `0`, `null`, or is undefined, defaulting to an infinite wait or a global default.
*   **State Management**: Ensure that if a step times out, it doesn't leave the system in an inconsistent state. The original `action` might still complete in the background; decide if its result should be discarded or logged for audit.
*   **Fallback Failure**: If the `fallbackResponse` is dynamic or involves its own action, it could also fail. The ultimate fallback should be a simple, static message.
*   **DB Connection Failure**: The timeout logging mechanism should gracefully handle database connection errors without crashing the main workflow.


## Pre-analysis for US-327 (Gemini 2.5 Pro)
This user story describes a **Node.js/TypeScript** CLI tool, not a Frappe/Python app. The `npm run` command and `.ts` test file path indicate a JavaScript/TypeScript environment.

Here is the technical brief based on the provided JSON for that environment:

**Project:** Node.js/TypeScript CLI Tool

**1. Files to Create/Modify:**
*   **Create:** `scripts/analyze-keyword-freshness.ts` - This new file will contain the core logic for the CLI tool that parses logs and analyzes keyword usage.
*   **Create:** `src/__tests__/keyword-freshness.test.ts` - A new test suite to validate the analyzer's logic using mock log data.
*   **Modify:** `package.json` - Add the new `analyze:keyword-freshness` command to the `"scripts"` section to execute the new script.

**2. Key Modules & Data Sources:**
*   **Node.js `fs` module:** To read production log files provided via a command-line path.
*   **Intent Keyword Source:** The script will need to read the master list of intent keywords. These are likely stored in a configuration file or within the `.rainbow-kb/` directory structure.
*   **CLI Argument Parser:** A library like `yargs` or `commander` will be needed to parse the log file path and `--days` flag.

**3. Recommended Implementation Approach:**
1.  In `package.json`, add: `"analyze:keyword-freshness": "ts-node scripts/analyze-keyword-freshness.ts"`.
2.  In `scripts/analyze-keyword-freshness.ts`, implement the main function that:
    a. Parses the CLI arguments for the log file path and days threshold.
    b. Loads all defined intent keywords into memory.
    c. Reads the specified log file line-by-line to handle large files efficiently.
    d. For each log entry within the date range, try to match its message against the list of all keywords.
    e. Track which keywords get matched and which messages fail to match any keyword.
3.  After processing, write two output files: `stale_keywords.txt` (for keywords with 0 matches) and `coverage_gaps.txt` (for messages with 0 matches).

**4. Edge Cases & Gotchas:**
*   **Performance:** Log files can be very large. The script must use streaming (e.g., `readline`) to avoid loading entire files into memory.
*   **Log Format:** The log parsing logic must be robust to handle potential inconsistencies or errors in the log's structure.
*   **Date Filtering:** Ensure correct date parsing and timezone handling when filtering logs from the last N days.
*   **Matching Logic:** The keyword matching algorithm (e.g., case-sensitivity, stemming) should precisely mirror the production intent detection logic to ensure the analysis is accurate.


## Pre-analysis for US-327 (Gemini 2.5 Pro)
This technical brief is for a **TypeScript/Node.js** project, as the user story references `npm` and `.ts` files.

### Technical Brief: US-327 Keyword Freshness Analyzer

**1. Key Files to Create/Modify:**
*   **`scripts/analyze-keyword-freshness.ts`**: (New) The core CLI script containing the analysis logic.
*   **`src/__tests__/keyword-freshness.test.ts`**: (New) As specified in the AC, for unit/integration testing the analyzer.
*   **`package.json`**: Modify the `scripts` section to add the new `analyze:keyword-freshness` command.
*   **`stale_keywords.txt` / `coverage_gaps.txt`**: (New) Output files generated by the script.

**2. Key Data Sources & Modules:**
*   **Intent Keywords:** The primary data source will be the set of intent keywords. These are likely sourced from markdown files within the `.rainbow-kb/` directory. The script must parse these files to build a master list of keywords.
*   **Production Logs:** The script will consume log files (format to be determined, likely JSON or plain text) from a path provided as a CLI argument.

**3. Recommended Implementation Approach:**
1.  Use a library like `yargs` to define and parse CLI arguments (`logPath`, `days`).
2.  Create a module to glob and parse keyword definitions from the `.rainbow-kb/**/*.md` files.
3.  Implement the main script to stream-read and parse log files from the specified `logPath`.
4.  For each log entry within the `days` window, iterate through the keyword list to find a match. Maintain a frequency map (`Map<string, number>`) for keyword hits.
5.  Collect any messages that do not match any keyword into a separate list for the coverage gap report.
6.  After processing, identify keywords with zero hits as "stale."
7.  Write the results to `stale_keywords.txt` and `coverage_gaps.txt`.

**4. Edge Cases & Gotchas:**
*   **Log Parsing:** Log format may be inconsistent; the parser must be robust.
*   **Performance:** Use streaming (e.g., `fs.createReadStream` and `readline`) to handle large log files without consuming excessive memory.
*   **Date Handling:** Ensure correct date parsing from logs, accounting for potential timezone differences.
*   **Matching Logic:** The keyword matching logic (e.g., case-insensitivity, whole word) should mirror the production intent detection engine's behavior for accurate analysis.
*   **Coverage Gaps:** Simply listing all unique unmatched messages might be noisy. Consider basic clustering or frequency analysis of unmatched phrases for a more actionable report.

## Iteration 1 - Tue, Mar 24, 2026  5:17:42 AM
FAILED diagnosis-block gate: Build Intent Keyword Freshness Analyzer CLI for Coverage Gap Detection (ID: US-327) — no diagnosis block found — attempt 1/3


## Pre-analysis for US-327 (Gemini 2.5 Pro)
This user story describes a TypeScript/Node.js CLI tool, not a Frappe/Python one. The technical brief is based on the project's actual stack.

**Project:** `rainbow-ai` (TypeScript/Node.js)
**User Story:** US-327 - Intent Keyword Freshness Analyzer

### Technical Brief

**1. Files to Modify/Create:**

*   **New Script:** `scripts/analyze-keyword-freshness.ts` will contain the core logic for the CLI tool.
*   **New Test:** `tests/keyword-freshness.test.ts` will house unit and integration tests for the analyzer.
*   **Configuration:** `package.json` must be updated to add the new `analyze:keyword-freshness` npm script.
*   **Output:** The script will generate `stale_keywords.txt` and `coverage_gaps.txt`.

**2. Involved Systems (No Frappe DocTypes):**

*   The script will interact with the file system to read production log files and likely the `.rainbow-kb/` directory to get the master list of intent keywords.
*   It will use Node.js core modules (`fs`) and potentially a CLI argument parser like `yargs`.

**3. Recommended Implementation Approach:**

1.  Use `yargs` to define and parse CLI arguments (`log-path`, `days`).
2.  Load all intent keywords from the `.rainbow-kb` structure into a master `Set`.
3.  Recursively read and stream log files from the provided path.
4.  For each log entry within the `days` threshold, check if the message matches any keyword. Maintain a `Map` to count matches for each keyword.
5.  Log messages that don't match any keyword into a "coverage gaps" list.
6.  Compare the master keyword `Set` with the match-count `Map` to identify keywords with zero matches ("stale keywords").
7.  Write the results to `stale_keywords.txt` and `coverage_gaps.txt`.

**4. Edge Cases & Gotchas:**

*   **Log Format Consistency:** The script will be brittle if the log message format is inconsistent; parsing must be robust.
*   **Performance:** Use streaming APIs for reading large log files to avoid high memory usage.
*   **Date/Timezone:** Ensure correct handling of log timestamps and date comparisons, as timezone issues are common.
*   **Matching Logic:** The keyword matching algorithm (e.g., case-sensitivity, whole/partial word) must precisely mirror the production implementation to be accurate.


## Iteration 14 - US-327: Build Intent Keyword Freshness Analyzer CLI for Coverage Gap Detection — PASSED

### What was implemented
**Core CLI Script** (`scripts/analyze-keyword-freshness.ts`):
- Loads keywords from all profiles (pelangi, makan, southern)
- Parses JSON/plain-text logs with optional date window filtering
- Matches messages against keywords (case-insensitive, substring matching)
- Identifies stale keywords (zero matches) and coverage gaps (unmatched messages)
- Outputs `stale_keywords.txt` and `coverage_gaps.txt`

**Test Suite** (`src/__tests__/keyword-freshness.test.ts`):
- 15 unit tests across 8 test cases
- Validates detection with 100+ realistic messages spanning 10 intents
- Tests: stale detection, coverage gaps, multilingual support, case-insensitivity, keyword counting
- All tests pass (15/15)

**Integration**:
- npm script `analyze:keyword-freshness` already present in package.json (line 40)
- CLI usage: `npm run analyze:keyword-freshness /path/to/logs/ --days 30`

### Acceptance criteria met
✓ **AC1**: CLI command parses logs and measures per-keyword match frequency
✓ **AC2**: Reports output stale_keywords.txt and coverage_gaps.txt
✓ **AC3**: Test file validates detection with 100+ messages spanning 10 intents
✓ **Build**: esbuild passes (1 pre-existing warning in logger.ts, not related)
✓ **Tests**: 15/15 tests pass

### Patterns discovered
- Keywords loaded from multiple profiles using Map<intent, keywords[]>
- Matching uses case-insensitive substring + normalization (trim + lowercase)
- Log parsing supports both JSON (with message/text/content fields) and plain text
- Date window filtering uses cutoff date calculation (now - days)
- Stale keywords identified via frequency count = 0

### Gotchas
- Windows NODE_OPTIONS env var doesn't work — use npx directly instead
- Log parsing gracefully handles both JSON and plain text formats
- All 70 intents loaded from profiles on startup (comprehensive coverage)


## Pre-analysis for US-328 (Gemini 2.5 Pro)
Based on the user story JSON, this is a TypeScript task for an AI assistant, not a Frappe/Python one. Here is the technical brief for the implementing agent:

**Project:** AI Assistant (TypeScript/Node.js)
**Objective:** Implement conversation summarization to prevent token limit errors in long conversations.

**1. Files to Modify:**
*   `src/assistant/context-manager.ts`: Create the new `summarizeConversationContext` function here.
*   `src/pipelines/intent-classification.ts` (Likely): This is the probable location to integrate the call to `summarizeConversationContext` before passing context to the AI model.
*   `tests/assistant/context-manager.test.ts`: Add a new unit test file or extend an existing one to validate the summarization logic.

**2. Involved Modules/Hooks:**
*   **Module:** `context-manager`: Will house the core summarization logic.
*   **Pipeline:** "Intent Classification Pipeline": This process needs to be hooked into to apply the summarization logic before the context is sent to the final AI provider.

**3. Implementation Approach:**
1.  In `src/assistant/context-manager.ts`, implement `summarizeConversationContext(messages, maxHistory = 10)`.
2.  If `messages.length > maxHistory`, split the array into `messagesToSummarize` and `recentMessages`.
3.  Make a separate, internal LLM call (e.g., to a fast model like Claude Haiku) with a prompt to distill `messagesToSummarize` into key facts (guest name, dates, room type, etc.).
4.  Modify the intent classification pipeline to call this function. It should then construct a new context payload that includes the returned `summary` text block followed by the `recentMessages`.
5.  Add logging to record when a summary is triggered and the message reduction ratio.

**4. Edge Cases & Gotchas:**
*   **Summarization Failure:** If the summarization LLM call fails, the function should gracefully fall back to simple truncation to avoid breaking the entire flow.
*   **Critical Detail Loss:** The summarization prompt must be engineered carefully to ensure it reliably extracts all booking-critical facts. Losing a detail like "no peanuts" could be a critical failure.
*   **Latency:** The extra LLM call will add latency. Monitor performance and ensure the summarization model is fast.
*   **Contextual Awareness:** The main AI prompt must be updated to understand that the context now consists of a summary preamble plus recent messages.


## Iteration 15 - Mon, Mar 24, 2026 - Story: US-328 Summarize Long Conversation History to Prevent Multi-Turn Token Breaches — PASSED

### What was implemented

**Core Function** (`src/assistant/pipeline/context-manager.ts`):
- Added `summarizeConversationContext(messages, maxHistoryMessages=10)` function
- Returns `{summary, recentMessages, messageReductionRatio, factsExtracted}`
- Extracts booking-critical facts: guest name, room type, check-in/out dates, guest count, special requests
- Keeps only the most recent `maxHistoryMessages` (default 10) while building a summary from older messages
- Logs message reduction ratio with structured telemetry

**Fact Extraction Helper** (`extractBookingCriticalFacts()`):
- Pattern-based extraction for:
  - Guest name: "my name is", "I am", "I'm", "call me", "this is" patterns
  - Room type: "double", "single", "twin", "suite", "dorm", "family", "studio" (flexible with/without "room")
  - Guest count: "\d+ guests/people/persons"
  - Check-in/out dates: Flexible date pattern matching (March 15, 2025-03-15, etc.)
  - Special requests: Allergies, accessibility, floor preferences, quiet room, etc.

**Test Suite** (`src/assistant/pipeline/context-manager.test.ts`):
- 12 comprehensive tests validating summarization functionality
- Critical test: 15-message conversation condensed to 1 summary + 10 recent messages
- Tests verify preservation of all booking-critical details
- Tests for message reduction ratio calculation (0-1 scale)
- Tests for empty message handling and within-limit conversations
- All tests passing ✓

### Acceptance Criteria Met

✓ **AC1**: Function `summarizeConversationContext(messages, maxHistoryMessages=10)` in `src/assistant/pipeline/context-manager.ts`
✓ **AC2**: Extracts key facts (guest name, room type, dates, special requests) and returns `{summary, recentMessages}`
✓ **AC3**: 15-message conversation is condensed to 1 summary + 10 recent messages while preserving all booking-critical details (room type, check-in date, guest count)
✓ **AC4**: Unit tests with 100+ realistic messages spanning multiple intents and languages
✓ **AC5**: Logs summary generation with message reduction ratio

### Patterns Discovered

- Regex patterns designed for flexibility to handle various conversational phrasings
- Guest name extraction prioritizes first user message with name introduction
- Room type extraction works with or without "room" word following type
- Date extraction handles multiple formats (natural language + ISO)
- Special requests patterns intentionally permissive to catch edge cases
- Fact extraction gracefully handles missing or malformed data

### Gotchas & Implementation Notes

- Pattern-based extraction is "best effort" — not all variations are captured (e.g., "I want John's room")
- Summary is only generated when messages exceed maxHistoryMessages limit
- messageReductionRatio formula: (original count - recent count) / original count
- Fact extraction runs on combined text of ALL messages, so later mentions override earlier ones
- specialRequests array is deleted if empty to keep factsExtracted clean

### Quality Checks

- ✓ 12/12 tests pass
- ✓ Build clean (1 pre-existing warning in logger.ts)
- ✓ Server startup verified on port 3002
- ✓ No new TypeScript errors introduced
- ✓ Integration with existing context-manager.ts functions

### Design Notes

The function is a utility that can be called from the intent classification pipeline (currently applySummarization in `src/assistant/pipeline/stages/summarization.ts`), but is implemented at a lower level in context-manager.ts for reusability. The fact extraction is conservative (no assumptions, pattern-based only) to avoid hallucinations.

