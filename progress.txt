
## Pre-analysis for US-537 (Gemini 2.5 Pro)
Based on the user story JSON for **US-537**, here is the technical brief for the implementation.

**Note:** The user story describes a **TypeScript** module, not a Frappe/Python one. The analysis below is based on the provided JSON.

---

### **Technical Brief: US-537 Kreuzberg Document Extraction Module**

**1. Primary Files to Modify/Create:**
*   `src/lib/kb-extractor.ts`: New TypeScript module to house the extraction logic.
*   `src/lib/__tests__/kb-extractor.test.ts`: New Vitest unit test for the module.
*   (Optional) `scripts/extract_wrapper.py`: A Python script could be created if the `child_process` approach is chosen to wrap the `kreuzberg` CLI, isolating the Python dependency.

**2. Frappe DocTypes / Hooks:**
*   None. This is a standalone Node.js/TypeScript module and does not directly interact with the Frappe framework.

**3. Recommended Implementation Approach:**
*   In `src/lib/kb-extractor.ts`, create the `extractToMarkdown` async function.
*   Use Node's `child_process.spawn` to execute the `kreuzberg` command-line tool, passing the `filePath` as an argument.
*   For image files, add the `--ocr` flag to the `kreuzberg` command based on the function's `opts`.
*   Capture the Markdown output from the process's `stdout` stream.
*   Define a custom `KBExtractorError` class extending `Error` for structured error handling.
*   In the test file, use sample fixtures (e.g., a simple `.txt`, `.docx`, `.png`) to validate that the function returns a non-empty string and handles errors correctly.

**4. Edge Cases & Gotchas:**
*   **Dependency:** `kreuzberg` must be installed and accessible in the system's PATH where the Node.js process runs. OCR may require separate dependencies like Tesseract.
*   **Performance:** Spawning a child process for each file can be slow. If high throughput is needed, a more integrated library or a persistent service might be better.
*   **File Formats:** Markdown conversion quality can vary, especially for complex PDFs or spreadsheets. The output may require further cleaning.
*   **Error Handling:** The `kreuzberg` tool might fail silently or return cryptic errors. The wrapper must reliably catch `stderr` and process exit codes.

---

## Iteration [1] - Story: Add Kreuzberg Document Extraction Module for KB Ingestion (US-537)

### What was implemented
- Created `src/lib/kb-extractor.ts` module that:
  - Exports `extractToMarkdown(filePath: string, opts?: { ocr?: boolean }): Promise<string>`
  - Uses `child_process.spawn` to call `kreuzberg extract` CLI with `--output-format markdown`
  - Adds `--ocr true` flag when `opts.ocr` is true
  - Captures stdout and returns extracted markdown
  - Throws descriptive `KBExtractorError` for failures

- Created `src/lib/__tests__/kb-extractor.test.ts` with 11 passing tests:
  - Extract from plain text files
  - Clean markdown output validation
  - Error handling for non-existent files
  - OCR option support
  - Empty file detection
  - Special characters in file paths
  - KBExtractorError class validation

### Patterns discovered
- Variable shadowing issue: importing `process` from `process` module while also using child process reference
  - Fixed by renaming child process variable from `process` to `child` to avoid shadowing
- kreuzberg CLI is available in PATH at `/c/Users/Jyue/.local/bin/kreuzberg`
- All tests use temporary directory cleanup with beforeEach/afterEach hooks
- Test fixtures use simple text files to verify extraction returns non-empty markdown

### Gotchas
- Had to use import process from 'process' but then rename the spawn result to avoid shadowing
- kreuzberg returns exit code 0 on success; non-zero codes indicate failure (checked in close event)
- Empty output from kreuzberg is treated as an error (corrupted/unsupported file)

### Quality checks
✅ Build: esbuild successful (2.6mb bundle)
✅ Tests: 11/11 passing in kb-extractor.test.ts
✅ Server startup: tsx runs without errors
✅ prd.json: Updated US-537.passes to true

## Iteration 1 - Mon, Apr 13, 2026 11:14:53 AM
FAILED diagnosis-block gate: Add Kreuzberg Document Extraction Module for KB Ingestion (ID: US-537) — no diagnosis block found — attempt 1/3


## Pre-analysis for US-537 (Gemini 2.5 Pro)
Based on the user story JSON for **US-537**, here is the technical brief for the implementation.

**Note:** The user story describes a **TypeScript** module, not a Frappe/Python one. The analysis below is based on the provided JSON.

---

### **Technical Brief: US-537 Kreuzberg Document Extraction Module**

**1. Primary Files to Modify/Create:**
*   `src/lib/kb-extractor.ts`: New TypeScript module to house the extraction logic.
*   `src/lib/__tests__/kb-extractor.test.ts`: New Vitest unit test for the module.
*   (Optional) `scripts/extract_wrapper.py`: A Python script could be created if the `child_process` approach is chosen to wrap the `kreuzberg` CLI, isolating the Python dependency.

**2. Frappe DocTypes / Hooks:**
*   None. This is a standalone Node.js/TypeScript module and does not directly interact with the Frappe framework.

**3. Recommended Implementation Approach:**
*   In `src/lib/kb-extractor.ts`, create the `extractToMarkdown` async function.
*   Use Node's `child_process.spawn` to execute the `kreuzberg` command-line tool, passing the `filePath` as an argument.
*   For image files, add the `--ocr` flag to the `kreuzberg` command based on the function's `opts`.
*   Capture the Markdown output from the process's `stdout` stream.
*   Define a custom `KBExtractorError` class extending `Error` for structured error handling.
*   In the test file, use sample fixtures (e.g., a simple `.txt`, `.docx`, `.png`) to validate that the function returns a non-empty string and handles errors correctly.

**4. Edge Cases & Gotchas:**
*   **Dependency:** `kreuzberg` must be installed and accessible in the system's PATH where the Node.js process runs. OCR may require separate dependencies like Tesseract.
*   **Performance:** Spawning a child process for each file can be slow. If high throughput is needed, a more integrated library or a persistent service might be better.
*   **File Formats:** Markdown conversion quality can vary, especially for complex PDFs or spreadsheets. The output may require further cleaning.
*   **Error Handling:** The `kreuzberg` tool might fail silently or return cryptic errors. The wrapper must reliably catch `stderr` and process exit codes.


## Iteration [2] - Story: Add Kreuzberg Document Extraction Module for KB Ingestion (US-537)
### What was implemented
- `src/lib/kb-extractor.ts`: TypeScript wrapper around `kreuzberg` CLI via `child_process.spawn`
  - Exports `extractToMarkdown(filePath, opts?) => Promise<string>`
  - Exports `KBExtractorError` with `filePath` property
  - Passes `--ocr true` flag when `opts.ocr` is set
  - Rejects with `KBExtractorError` on non-zero exit, spawn error, or empty output
- `src/lib/__tests__/kb-extractor.test.ts`: 11 vitest tests covering extraction, error cases, OCR flag, KBExtractorError class

### Patterns discovered
- kreuzberg CLI is available at `/c/Users/Jyue/.local/bin/kreuzberg`
- Build: esbuild 2.6mb bundle (pre-existing warnings only)
- All 11 tests pass including extraction from .txt fixtures and error handling

### Quality checks
✅ Build: esbuild successful (2.6mb bundle, 1 pre-existing warning)
✅ Tests: 11/11 passing in kb-extractor.test.ts
✅ prd.json: US-537.passes = true
## Iteration 2 - Mon, Apr 13, 2026 11:17:37 AM
Completed: Add Kreuzberg Document Extraction Module for KB Ingestion (ID: US-537) in 2m


## Pre-analysis for US-538 (Gemini 2.5 Pro)
**Correction:** The user story specifies a **Node.js/TypeScript** implementation, not Frappe/Python. The technical brief below is for the specified Node.js/TypeScript environment.

---

**To:** Claude Code Agent
**From:** Gemini Preparatory Agent
**Subject:** Technical Brief for US-538: Admin KB Document Upload

Here is the plan for implementing the KB document upload endpoint.

1.  **Files to Modify:**
    *   Create `src/routes/admin/kb-upload.ts` to house the new endpoint logic.
    *   Update `src/routes/admin/index.ts` to import and register the new router under the `/kb` path.
    *   You will call the `extractToMarkdown()` function, which is expected to exist in `src/lib/kb-extractor.ts`.

2.  **Framework/Entities Involved:**
    *   **Framework:** Express.js (or similar Node.js web framework).
    *   **Middleware:** Use `multer` or a similar library to handle the `multipart/form-data` upload.
    *   **Key Objects:** `KnowledgeBaseInstance` (for `reloadKBFile` and `reindexKB` calls).

3.  **Implementation Approach:**
    *   In `kb-upload.ts`, define an Express router for the `POST /:profile/upload` route.
    *   Use a `multer` middleware to process the uploaded `file` and place it in a temporary directory.
    *   Validate the `:profile` param against `['pelangi', 'southern', 'makan']`; throw a 400 error if invalid.
    *   In a `try/catch` block, call `extractToMarkdown()` with the temp file path.
    *   Construct the destination path (e.g., `.rainbow-kb-southern/your-filename.md`).
    *   Write the extracted markdown content to the destination file.
    *   Call `reloadKBFile()` and `reindexKB()` on the relevant `KnowledgeBaseInstance`.
    *   Return a 200 response with `{ success: true, kbFile: '...', extractedChars: ..., ragRebuilt: true }`.

4.  **Edge Cases & Gotchas:**
    *   **Extraction Failure:** Catch errors from `extractToMarkdown()` and return a 422 status code with the error message.
    *   **File Naming:** Sanitize the optional `filename` from the request to prevent path traversal vulnerabilities. If not provided, generate a safe filename from the original upload name.
    *   **Cleanup:** Ensure the temporary file created by `multer` is deleted after extraction is complete, even if an error occurs.
    *   **RAG Indexing:** The `reindexKB()` call might be slow. The operation should be handled asynchronously without blocking the response. The `ragRebuilt: true` flag confirms its completion.


## Iteration [3] - Story: Add Admin API Endpoint for KB Document Upload and Extraction (US-538)

### What was implemented
- Created `src/routes/admin/kb-upload.ts`:
  - POST `/:profile/upload` endpoint accepting multipart/form-data
  - Profile validation: pelangi, southern, makan (mapped to internal profile IDs)
  - Filename sanitization with `sanitizeFilename()` function to prevent path traversal
  - Uses multer with memoryStorage (50MB max file size)
  - Calls `extractToMarkdown()` from kb-extractor.ts
  - Writes extracted markdown to profile's KB directory
  - Calls `reloadKBFile()` to refresh in-memory cache
  - Calls new public `reindexKB()` method to rebuild RAG index
  - Returns JSON: { success, kbFile, extractedChars, ragRebuilt }
  - Handles 422 status for extraction failures, 400 for invalid profiles
  - Proper cleanup of temp files in finally block

- Updated `src/assistant/knowledge-base-instance.ts`:
  - Added public `async reindexKB(): Promise<void>` method (US-538/US-542)
  - Wraps existing private `rebuildRAGIndex()` method
  - Enables kb-upload route to trigger RAG index rebuild after KB file written

- Updated `src/routes/admin/index.ts`:
  - Imported kb-upload router
  - Registered at `/kb` path: `router.use('/kb', kbUploadRoutes)`
  - Full endpoint path: `/api/admin/kb/:profile/upload`

### Patterns discovered
- KnowledgeBaseInstance exposes kbDir as public readonly property for path resolution
- Profile registry maps internal profile IDs to user-facing names
- ResolvedProfile contains configStore and kb (KnowledgeBaseInstance) for admin operations
- Filename validation reuses existing `validateFilename()` helper from http-utils.ts
- RAG rebuilding is async and non-blocking; upload response doesn't wait for completion

### Edge cases handled
- Profile aliasing: URL param "makan" → internal ID "makan-moments"
- Empty/default filename: generates "untitled.md" if no name provided
- Path traversal prevention: sanitizeFilename strips slashes, dots, and non-alphanumeric chars
- Extraction failure: returns 422 with error message from KBExtractorError
- RAG rebuild failure: logs warning but doesn't fail the upload
- Temp file cleanup: uses finally block to delete temp file even if errors occur

### Quality checks
✅ Build: esbuild successful (2.6mb bundle, 1 pre-existing warning)
✅ Type check: No errors in kb-upload.ts, knowledge-base-instance.ts
✅ Pre-existing errors: context-window-benchmark.ts (expected, noted in CLAUDE.md)
✅ Server startup: tsx runs without errors
✅ prd.json: US-538.passes = true

## Iteration 3 - Mon, Apr 13, 2026 11:25:52 AM
FAILED diagnosis-block gate: Add Admin API Endpoint for KB Document Upload and Extraction (ID: US-538) — no diagnosis block found — attempt 1/3


## Pre-analysis for US-538 (Gemini 2.5 Pro)
**Correction:** The user story specifies a **Node.js/TypeScript** implementation, not Frappe/Python. The technical brief below is for the specified Node.js/TypeScript environment.

---

**To:** Claude Code Agent
**From:** Gemini Preparatory Agent
**Subject:** Technical Brief for US-538: Admin KB Document Upload

Here is the plan for implementing the KB document upload endpoint.

1.  **Files to Modify:**
    *   Create `src/routes/admin/kb-upload.ts` to house the new endpoint logic.
    *   Update `src/routes/admin/index.ts` to import and register the new router under the `/kb` path.
    *   You will call the `extractToMarkdown()` function, which is expected to exist in `src/lib/kb-extractor.ts`.

2.  **Framework/Entities Involved:**
    *   **Framework:** Express.js (or similar Node.js web framework).
    *   **Middleware:** Use `multer` or a similar library to handle the `multipart/form-data` upload.
    *   **Key Objects:** `KnowledgeBaseInstance` (for `reloadKBFile` and `reindexKB` calls).

3.  **Implementation Approach:**
    *   In `kb-upload.ts`, define an Express router for the `POST /:profile/upload` route.
    *   Use a `multer` middleware to process the uploaded `file` and place it in a temporary directory.
    *   Validate the `:profile` param against `['pelangi', 'southern', 'makan']`; throw a 400 error if invalid.
    *   In a `try/catch` block, call `extractToMarkdown()` with the temp file path.
    *   Construct the destination path (e.g., `.rainbow-kb-southern/your-filename.md`).
    *   Write the extracted markdown content to the destination file.
    *   Call `reloadKBFile()` and `reindexKB()` on the relevant `KnowledgeBaseInstance`.
    *   Return a 200 response with `{ success: true, kbFile: '...', extractedChars: ..., ragRebuilt: true }`.

4.  **Edge Cases & Gotchas:**
    *   **Extraction Failure:** Catch errors from `extractToMarkdown()` and return a 422 status code with the error message.
    *   **File Naming:** Sanitize the optional `filename` from the request to prevent path traversal vulnerabilities. If not provided, generate a safe filename from the original upload name.
    *   **Cleanup:** Ensure the temporary file created by `multer` is deleted after extraction is complete, even if an error occurs.
    *   **RAG Indexing:** The `reindexKB()` call might be slow. The operation should be handled asynchronously without blocking the response. The `ragRebuilt: true` flag confirms its completion.


## Iteration 4 - Mon, Apr 13, 2026 (Retry Attempt 2)
### Story: Add Admin API Endpoint for KB Document Upload and Extraction (US-538)

### What was implemented
Previous attempt (Iteration 3) had already written all the code correctly:
- `src/routes/admin/kb-upload.ts`: Full endpoint with multer memoryStorage, profile validation, kreuzberg extraction, KB write, reloadKBFile, reindexKB, 422 for extraction failures, temp file cleanup
- `src/lib/kb-extractor.ts`: extractToMarkdown() spawning kreuzberg CLI process
- `src/routes/admin/index.ts`: Route registered at /kb (line 343)
- `src/assistant/knowledge-base-instance.ts`: reindexKB() public method wrapping rebuildRAGIndex()

The previous attempt failed ONLY because the SPIRAL diagnosis-block gate rejected it for missing the diagnosis block output. This iteration provided the diagnosis block first, verified the existing code, and marked passes = true.

### Patterns discovered
- SPIRAL requires explicit "## Current State / ## Problem Identified / ## Planned Changes" block BEFORE any edits
- On retry attempts, always check if previous code was already written before re-implementing

### Quality checks
✅ Build: esbuild successful (2.6mb bundle, 1 pre-existing warning in logger.ts)
✅ Server startup: tsx runs without errors
✅ prd.json: US-538.passes = true
## Iteration 4 - Mon, Apr 13, 2026 11:28:49 AM
Completed: Add Admin API Endpoint for KB Document Upload and Extraction (ID: US-538) in 2m


## Pre-analysis for US-542 (Gemini 2.5 Pro)
Here is the technical brief for user story US-542.

**Project Context:** The user story targets a **TypeScript/Node.js** application. The plan below is based on the provided JSON, not the Frappe/Python context mentioned in the prompt.

**1. Python Files to Modify:**
No Python files are involved. The following TypeScript files will be modified:
*   `src/assistant/knowledge-base-instance.ts`
*   `src/routes/admin/kb-upload.ts`
*   `src/tools/kb-ingest-cli.ts`

**2. Frappe DocTypes / Hooks:**
*   None. This is a Node.js project using custom classes and likely an Express-based API.

**3. Recommended Implementation Approach:**
*   In `KnowledgeBaseInstance`, define `public async reindexKB(): Promise<void>`. This method should `await` the existing private `rebuildRAGIndex()` within a `try...catch` block.
*   In the `kb-upload.ts` route, after the file is written and `reloadKBFile()` is called, `await` the new `reindexKB()` method.
*   Modify the upload API's JSON response to include `ragRebuilt: true` upon successful re-indexing.
*   In the `kb-ingest-cli.ts` tool, `await reindexKB()` after successful ingestion and then print "RAG index rebuilt."

**4. Edge Cases and Gotchas:**
*   **Asynchronicity:** Ensure the `reindexKB` call is properly awaited in both the API route and the CLI tool.
*   **Error Handling:** A failure in `rebuildRAGIndex` should be caught and logged. The API response should reflect this status (e.g., `ragRebuilt: false`).
*   **Concurrency:** Simultaneous uploads could trigger multiple rebuilds. Consider adding a simple state lock (`isRebuilding`) in `KnowledgeBaseInstance` to prevent race conditions.


## Pre-analysis for US-541 (Gemini 2.5 Pro)
Based on the user story JSON, this is a **Node.js/TypeScript** project, not a Frappe/Python one. The implementation should target the specified `.ts` files.

**Technical Brief: US-541 OCR Detection for PDF Ingestion**

*   **Project Context:** This story enhances a TypeScript-based `kb-extractor` module to handle scanned PDFs by integrating an OCR option.

*   **Python Files to Modify:**
    *   N/A. This is a TypeScript project. The key files are:
    *   `src/lib/kb-extractor.ts` (core logic)
    *   `src/routes/admin/kb-upload.ts` (API endpoint)
    *   `src/tools/kb-ingest-cli.ts` (CLI tool)
    *   `src/lib/__tests__/kb-extractor.test.ts` (unit tests)

*   **Frappe DocTypes / Hooks:**
    *   N/A. This involves the `kb-extractor` module and its dependents.

*   **Implementation Approach:**
    1.  In `kb-extractor.ts`, update the `extractToMarkdown` function to accept an `options: { ocr?: boolean }` argument.
    2.  If `options.ocr` is `true`, route the PDF to the `kreuzberg` Tesseract OCR backend.
    3.  If not using OCR, check if the extracted text length is `< 50` chars. If so, log a `console.warn` suggesting the `--ocr` flag.
    4.  Update the `admin/kb-upload.ts` route to accept a `?ocr=true` query param and pass it to the extractor.
    5.  Add an `--ocr` flag to the `kb-ingest` CLI tool.
    6.  Create unit tests in `kb-extractor.test.ts` to verify the new OCR path and the short-text warning.

*   **Gotchas & Edge Cases:**
    *   Confirm the exact API call for `kreuzberg`'s OCR vs. standard extraction.
    *   Handle potential errors from the OCR process gracefully.
    *   The 50-character threshold may need tuning; it could be sensitive to cover pages or empty documents.


## Iteration [5] - Story: OCR Detection and Warning for Scanned PDF KB Ingestion (US-541)

### What was implemented
- Updated `src/lib/kb-extractor.ts`:
  - Added logic to warn when extracted text is < 50 characters and OCR not enabled
  - Logs: 'Warning: extracted text is very short -- consider re-running with --ocr'
  - Only warns when `!opts?.ocr` to avoid false positives when OCR is already being used

- Updated `src/routes/admin/kb-upload.ts`:
  - Accepts `?ocr=true` query parameter from HTTP request
  - Extracts param: `const useOcr = req.query.ocr === 'true'`
  - Passes OCR flag to extractToMarkdown: `extractToMarkdown(tempPath, { ocr: useOcr })`

- Updated `src/lib/__tests__/kb-extractor.test.ts`:
  - Added 3 new tests for US-541:
    1. Verify warning logs when text < 50 chars without OCR
    2. Verify no warning when text < 50 chars WITH OCR enabled
    3. Verify no warning when text >= 50 chars without OCR
  - Added `vi` import from vitest for spying on console.warn
  - Test description now includes US-541

### Patterns discovered
- console.warn can be mocked/spied on using `vi.spyOn(console, 'warn')` in vitest
- Query parameters in Express are accessed via `req.query` object
- The warning logic only triggers when both conditions met: text < 50 chars AND !opts.ocr
- All previous tests (for US-537, US-538) continue to pass without modification

### Edge cases handled
- OCR flag from query param must be explicitly 'true' string (falsy values, missing, 'false' all treated as false)
- Warning doesn't interfere with extraction — it logs but still resolves with the extracted markdown
- Pre-existing tests for normal length files don't trigger warning (confirmed in test output)

### Quality checks
✅ Build: esbuild successful (2.6mb bundle, 1 pre-existing warning in logger.ts)
✅ Tests: 14/14 passing in kb-extractor.test.ts (11 from US-537 + 3 from US-541)
✅ Server startup: tsx runs without errors
✅ prd.json: US-541.passes = true

## Iteration 5 - Mon, Apr 13, 2026 12:06:18 PM
Completed: OCR Detection and Warning for Scanned PDF KB Ingestion (ID: US-541) in 1m
## Iteration 1 - Mon, Apr 13, 2026 12:07:38 PM
FAILED diagnosis-block gate: OCR Detection and Warning for Scanned PDF KB Ingestion (ID: US-541) — no diagnosis block found — attempt 1/3


## Pre-analysis for US-541 (Gemini 2.5 Pro)
Based on the user story JSON, this is a **Node.js/TypeScript** project, not a Frappe/Python one. The implementation should target the specified `.ts` files.

**Technical Brief: US-541 OCR Detection for PDF Ingestion**

*   **Project Context:** This story enhances a TypeScript-based `kb-extractor` module to handle scanned PDFs by integrating an OCR option.

*   **Python Files to Modify:**
    *   N/A. This is a TypeScript project. The key files are:
    *   `src/lib/kb-extractor.ts` (core logic)
    *   `src/routes/admin/kb-upload.ts` (API endpoint)
    *   `src/tools/kb-ingest-cli.ts` (CLI tool)
    *   `src/lib/__tests__/kb-extractor.test.ts` (unit tests)

*   **Frappe DocTypes / Hooks:**
    *   N/A. This involves the `kb-extractor` module and its dependents.

*   **Implementation Approach:**
    1.  In `kb-extractor.ts`, update the `extractToMarkdown` function to accept an `options: { ocr?: boolean }` argument.
    2.  If `options.ocr` is `true`, route the PDF to the `kreuzberg` Tesseract OCR backend.
    3.  If not using OCR, check if the extracted text length is `< 50` chars. If so, log a `console.warn` suggesting the `--ocr` flag.
    4.  Update the `admin/kb-upload.ts` route to accept a `?ocr=true` query param and pass it to the extractor.
    5.  Add an `--ocr` flag to the `kb-ingest` CLI tool.
    6.  Create unit tests in `kb-extractor.test.ts` to verify the new OCR path and the short-text warning.

*   **Gotchas & Edge Cases:**
    *   Confirm the exact API call for `kreuzberg`'s OCR vs. standard extraction.
    *   Handle potential errors from the OCR process gracefully.
    *   The 50-character threshold may need tuning; it could be sensitive to cover pages or empty documents.


## Iteration [6] - Story: OCR Detection and Warning for Scanned PDF KB Ingestion (US-541)

### What was implemented
All implementation was already complete from Iteration 5. This iteration (attempt 2) output the required diagnosis block first, then verified the existing implementation.

- `src/lib/kb-extractor.ts`: OCR option accepted, short-text warning fires when text < 50 chars and `!opts?.ocr`
- `src/routes/admin/kb-upload.ts`: `?ocr=true` query param parsed and forwarded to `extractToMarkdown`
- `src/tools/kb-ingest-cli.ts`: `--ocr` flag wired through `parseArgs` → `ingestDocument` → `extractToMarkdown`
- `src/lib/__tests__/kb-extractor.test.ts`: 3 tests verifying warning fires / doesn't fire based on text length and OCR flag

### Patterns discovered
- SPIRAL outer loop requires a diagnosis block (## Current State / ## Problem Identified / ## Planned Changes) as plain text BEFORE any file edits or Bash commands that modify files
- On Windows, `NODE_OPTIONS='...'` env prefix doesn't work in PowerShell/CMD; use `npx vitest run` directly
- 14/14 tests pass; build succeeds with 1 pre-existing warning (logger.ts CommonJS/ESM mismatch)

### Quality checks
✅ Build: esbuild successful (2.6mb, 1 pre-existing warning)
✅ Tests: 14/14 passing in kb-extractor.test.ts
✅ prd.json: US-541.passes = true
